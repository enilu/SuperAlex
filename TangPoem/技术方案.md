# 唐诗小当家（TangPoem Kids）技术方案

> 一款专为儿童设计的唐诗学习游戏，让孩子在游戏中快乐背诵唐诗

## 项目信息

| 项目 | 信息 |
|------|------|
| 英文名 | TangPoem Kids（简洁易记，适配 H5 小游戏） |
| 中文名 | 唐诗小当家（童趣 + 成就感，符合儿童心理） |
| 项目定位 | 儿童唐诗学习游戏化应用 |
| 目标用户 | 3-12岁儿童及家长 |

---

## 一、整体设计思路

核心是 **"游戏化 + 轻量化 + 儿童友好"**，以唐诗背诵、复习为核心，结合闯关、奖励、音效等游戏元素，适配手机端 H5，无复杂依赖，家长和孩子可即开即用。

### 设计原则

1. **游戏化优先**：将枯燥的唐诗背诵转化为有趣的闯关游戏
2. **轻量高效**：纯前端实现，无需安装，打开即用
3. **儿童友好**：界面简洁，操作简单，鼓励为主
4. **家长放心**：可控的学习时长和难度设置

---

## 二、核心功能模块

| 模块 | 核心功能 | 说明 |
|------|----------|------|
| **唐诗题库** | 内置 300 首唐诗（分学段 / 难度），包含原文、拼音、注释、译文、作者信息 | 按年级和难度分级 |
| **游戏模式** | 1. 诵读模式：带拼音逐句朗读，支持音频播放<br>2. 闯关模式：填空 / 选择 / 背诵答题<br>3. 复习模式：根据记忆曲线推荐待复习唐诗 | 多种模式满足不同学习阶段 |
| **进度管理** | 记录已学习 / 背诵 / 掌握的唐诗，展示学习进度条、成就勋章 | 可视化学习成果 |
| **交互反馈** | 答对音效 / 动画、鼓励语，答错温馨提示（不打击信心） | 正向激励机制 |
| **家长设置** | 可设置学习时长、难度、每日学习目标 | 家长可控管理 |
| **数据存储** | 本地存储学习进度、成就、答题记录（无需后端，纯前端） | 隐私安全，离线可用 |

---

## 三、技术栈

采用轻量化技术选型，无复杂依赖

| 技术分类 | 具体选型 | 说明 |
|----------|----------|------|
| **基础架构** | HTML5 + CSS3（响应式） + 原生 JavaScript（ES6+） | 零依赖，跨平台 |
| **样式优化** | Flex/Grid 布局（适配手机端）、CSS 变量（统一主题色）、简单动画（CSS keyframes） | 流畅的交互体验 |
| **音频处理** | Web Audio API（播放唐诗诵读音频、反馈音效） | 原生音频支持 |
| **数据存储** | localStorage（存储学习进度、设置、成就） | 本地持久化 |
| **辅助工具** | iconfont（SVG 图标）<br>axios（可选，若需后端接口） | 按需引入 |

---

## 四、项目目录结构

```
TangPoemKids/
├── index.html                # 主页面（游戏入口）
├── settings.html             # 家长设置页面
├── assets/
│   ├── css/
│   │   ├── reset.css         # 样式重置
│   │   ├── common.css        # 通用样式（主题、布局）
│   │   └── game.css          # 游戏页面样式
│   ├── js/
│   │   ├── config.js         # 配置文件（唐诗分类、难度、音效路径）
│   │   ├── poemDB.js         # 唐诗题库（JSON格式，含原文/拼音/注释）
│   │   ├── gameManager.js    # 游戏核心逻辑（模式切换、答题判断）
│   │   ├── progress.js       # 进度管理（存储/读取学习数据）
│   │   ├── audioManager.js   # 音频管理（诵读、音效播放）
│   │   ├── settings.js       # 设置页面逻辑
│   │   └── utils.js          # 工具函数（拼音匹配、随机出题等）
│   ├── audio/
│   │   ├── recite/           # 唐诗诵读音频（如 静夜思.mp3）
│   │   └── effects/          # 反馈音效（correct.mp3/encourage.mp3）
│   ├── images/
│   │   ├── icons/            # 功能图标（播放、暂停、返回等）
│   │   └── badges/           # 成就勋章图片（如 背诵小能手.png）
│   └── data/
│       └── poems.json        # 唐诗300首完整数据（JSON）
├── README.md                 # 项目说明
└── test.html                 # 功能测试页面
```

---

## 五、核心功能实现方案

### 5.1 唐诗题库设计（JSON 示例）

```json
// assets/data/poems.json
[
  {
    "id": 1,
    "title": "静夜思",
    "author": "李白",
    "dynasty": "唐",
    "grade": "小学一年级",
    "content": [
      {"text": "床前明月光", "pinyin": "chuáng qián míng yuè guāng"},
      {"text": "疑是地上霜", "pinyin": "yí shì dì shàng shuāng"},
      {"text": "举头望明月", "pinyin": "jǔ tóu wàng míng yuè"},
      {"text": "低头思故乡", "pinyin": "dī tóu sī gù xiāng"}
    ],
    "annotation": "霜：指地上像霜一样的月光",
    "translation": "明亮的月光洒在井栏上，好像地上结了一层霜。抬起头看天上的明月，低下头思念自己的故乡。",
    "audio": "assets/audio/recite/jingyesi.mp3"
  }
]
```

### 5.2 核心游戏模式实现逻辑

```javascript
// assets/js/gameManager.js

// 1. 诵读模式
function startReciteMode(poemId) {
  const poem = getPoemById(poemId);
  renderPoemWithPinyin(poem);
  playAudio(poem.audio);

  // 绑定逐句朗读按钮事件
  document.querySelectorAll('.poem-line').forEach((line, index) => {
    line.addEventListener('click', () => playLineAudio(poem.id, index));
  });
}

// 2. 闯关模式（填空示例）
function generateFillBlankQuestion(poem) {
  const randomLine = Math.floor(Math.random() * poem.content.length);
  const line = poem.content[randomLine];

  // 随机隐藏1-2个字词
  const words = line.text.split('');
  const blankIndex = Math.floor(Math.random() * words.length);
  const answer = words[blankIndex];
  words[blankIndex] = '____';

  // 生成题目和选项
  return {
    question: `${poem.title} 第${randomLine+1}句：${words.join('')}`,
    options: [answer, getRandomWrongWord(), getRandomWrongWord()].sort(),
    correctAnswer: answer,
    poemId: poem.id
  };
}

// 3. 复习模式（基于记忆曲线）
function getReviewPoems() {
  const learnedPoems = JSON.parse(localStorage.getItem('learnedPoems')) || [];

  // 筛选出需要复习的唐诗（如2天前学习、未掌握的）
  return learnedPoems.filter(poem => {
    const lastReviewTime = new Date(poem.lastReviewTime);
    const now = new Date();
    const daysDiff = (now - lastReviewTime) / (1000 * 60 * 60 * 24);
    return daysDiff >= 2 && poem.mastery < 100;
  });
}
```

### 5.3 进度与成就管理

```javascript
// assets/js/progress.js

// 记录学习进度
function updateProgress(poemId, action) {
  const progress = JSON.parse(localStorage.getItem('poemProgress')) || {};

  if (!progress[poemId]) {
    progress[poemId] = {
      learned: false,
      lastReviewTime: new Date().toISOString(),
      mastery: 0,
      correctCount: 0,
      wrongCount: 0
    };
  }

  // 根据操作更新进度
  if (action === 'correct') {
    progress[poemId].correctCount++;
    progress[poemId].mastery = Math.min(100, progress[poemId].mastery + 20);
  } else if (action === 'wrong') {
    progress[poemId].wrongCount++;
    progress[poemId].mastery = Math.max(0, progress[poemId].mastery - 10);
  }

  progress[poemId].lastReviewTime = new Date().toISOString();
  localStorage.setItem('poemProgress', JSON.stringify(progress));

  // 解锁成就
  checkAchievements();
}

// 成就解锁（如背诵10首解锁"小诗仙"）
function checkAchievements() {
  const totalMastered = Object.values(JSON.parse(localStorage.getItem('poemProgress')) || {})
    .filter(p => p.mastery === 100).length;

  const achievements = JSON.parse(localStorage.getItem('achievements')) || [];

  if (totalMastered >= 10 && !achievements.includes('小诗仙')) {
    achievements.push('小诗仙');
    localStorage.setItem('achievements', JSON.stringify(achievements));
    showAchievementToast('解锁成就：小诗仙！');
  }
}
```

---

## 六、运行与部署方案

### 6.1 本地运行（无需构建，纯静态）

```bash
# 方式1：Node.js静态服务器
npx serve .

# 方式2：Python静态服务器
python -m http.server 8080
```

### 6.2 部署方案

- **静态托管**：可直接部署到 GitHub Pages、Vercel、Netlify 等平台
- **CDN 加速**：音频资源可使用 CDN 加速加载
- **PWA 支持**：可添加 Service Worker 实现离线使用

---

## 七、技术难点与解决方案

| 难点 | 解决方案 |
|------|----------|
| 唐诗音频资源 | 使用开源TTS引擎合成，或寻找免费诵读资源 |
| 拼音标注准确性 | 建立多音字字典，根据上下文判断 |
| 记忆曲线算法 | 参考艾宾浩斯遗忘曲线，简化实现 |
| 移动端兼容 | 使用 Flexbox 布局，测试主流机型 |

---

**版本**：v1.0.0
**最后更新**：2026-02-15
**维护者**：SuperAlex Team
